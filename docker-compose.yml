services:
  replay:
    build: .
    image: tv-replay:latest
    ports:
      - "8090:8090"
    env_file:
      - "variables.env"
    volumes:
      - "/srv/library:/library:ro"
    restart: unless-stopped
    labels:
      - meta.role=replay
